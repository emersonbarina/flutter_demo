import 'package:flutter/material.dart';

class TAppTheme {
  static ThemeData lighTheme = ThemeData( brightness: Brightness.light );
  static ThemeData darkTheme = ThemeData( brightness: Brightness.dark );
}